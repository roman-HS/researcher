/**
 * Client-safe run polling defaults shared with the create-run API response.
 *
 * @see Story 7.3.2 — Implement idempotent run creation service
 * @see Story 8.2.4 — Add run status polling
 */

export const DEFAULT_RUN_POLL_AFTER_MS = 2000;
